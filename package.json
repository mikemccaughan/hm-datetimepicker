{
  "name": "hm-datetimepicker",
  "version": "1.0.0",
  "description": "",
  "main": "serve.mjs",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "node --test",
    "serve": "node ./serve.mjs",
    "build": "mkdir -p dist && cp -r *.html dist && cp -r *.js dist && cp -r *.css dist && cp -r *.json dist && cp -r *.mjs dist"
  },
  "author": "",
  "license": "ISC"
}
